/target/
/.worktrees/
*.pdb
*.log
.idea/
.vscode/

# CodeGraph index — rebuildable from source, per-machine
.codegraph/

# Cygwin/MSYS crash dumps — an artefact of the shell, never source
*.stackdump

# Real environment files — secrets. `.env.example` is the tracked placeholder.
.env
.env.*
!.env.example
